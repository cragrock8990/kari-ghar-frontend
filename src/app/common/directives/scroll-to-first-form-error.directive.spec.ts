import { ScrollToFirstFormErrorDirective } from './scroll-to-first-form-error.directive';

describe('ScrollToFirstFormErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToFirstFormErrorDirective();
    expect(directive).toBeTruthy();
  });
});
