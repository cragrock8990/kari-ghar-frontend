export interface City {
  name: string;
  code: string;
}

export interface Profession {
  name: string;
  code: string;
  icon: string;
}

export interface FooterLink {
  displayName: string;
  href: string;
}
